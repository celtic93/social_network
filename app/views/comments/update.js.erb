<% if @comment.errors.blank? %>
  $('#edit-form-comment-<%= @comment.id %>').addClass('hidden');
  $('#comment-<%= @comment.id %>').show().empty().append('<%= j render @comment %>');
<% else %>
  $('.comment-<%= @post.id %>-errors').html('<%= render 'shared/errors', resource: @comment %>');
<% end %>
