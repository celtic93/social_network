<% commented_id = "#{@commented.class.name.underscore}-#{@commented.id}" %>

$('.comment-<%= commented_id %>-errors').html('<%= render 'shared/errors', resource: @comment %>')

<% if @comment.persisted? %>
  $(".comments-<%= commented_id %>").append('<%= j render @comment %>');
  $("#comment-form-<%= commented_id %>").addClass('hidden');
  $("#comment-<%= commented_id %>").val('')
  $('.comment-post-link').show();
<% end %>
