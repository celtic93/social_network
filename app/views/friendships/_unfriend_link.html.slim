= link_to 'Unfriend', user_friendship_path(user_id: current_user.id, id: user.id),
                      class: 'btn btn-outline-primary',
                      method: :delete,
                      remote: true
