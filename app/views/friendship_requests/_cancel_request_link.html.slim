= link_to 'Cancel request', friendship_request_path(id: user.id),
                            class: 'btn btn-outline-primary',
                            method: :delete,
                            remote: true
